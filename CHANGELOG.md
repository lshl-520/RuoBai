# 更新记录（Changelog）

本文件记录本项目的**对外可见变化**，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 说明：本项目在正式引入版本号之前已有较长开发期，下方「早期开发」一节按主题汇总，
> 不再逐条对应提交。

---

## [Unreleased]

### 新增
- `DISCLAIMER.md`：免责声明与使用条款（年龄、合规、禁止用途、责任限制）。
- `SECURITY.md`、`CONTRIBUTING.md`、`.editorconfig`。
- 私有关系配置层：部署方可用私有文件覆盖角色称呼与关系设置，
  公开仓库只保留通用默认值。见 `docs/部署指南.md`「私有关系配置」。
- `docs/部署指南.md` 增补四节：`user_assets` 跨版本共享、前端构建须注入
  Live2D Cubism Core 地址、Android 15+ 安全区避让、私有关系配置。

### 修复
- 恢复 Live2D 实际播放：此前构建未注入 Cubism Core 地址，模型挂载会在约
  100ms 内失败并回退静态预览（表现为"立绘不动"）。
- 角色自身生成的图片不再作为图片内容发送给上游模型，
  避免上游因"助手消息携带图片"而拒绝整轮请求。
- 手机端系统栏遮挡修复：标题不再被状态栏压住，底部导航文字不再被导航栏覆盖。

### 文档
- `LICENSE` 补全 CC-BY-NC-4.0 官方全文与中文要点。
- `README.md` 顶部加入项目性质声明，并在结尾链接许可与免责全文。

---

## 早期开发（按主题汇总）

### 交互与人格
- **决策层（Decision Layer）**：在模型输出之前先判断意图，再由规则决定她怎么回应；
  共 13 类意图，含自伤/求生信号的安全例外。
- **她有自己的情绪与立场**：7 种心情、5 种立场；心情是叠加而非覆盖，
  且不机械映射对方情绪。
- **关系连续性**：分歧之后必须把"我还在"说出来；不过度道歉、不复盘、不推开对方。
- **边界规则**：不允许用冷战、离开、吃醋或沉默惩罚来操控关系。
- **表达层**：亲密场景使用日常口语、避免美化词与医学词；
  把话递到对方嘴边，减少"接不上"的情况。
- **安全例外**：识别到自伤/求生信号时，优先确认安全并提供求助渠道。

### 聊天
- 聊天历史分页（游标翻页，首屏与触底加载，无重叠）。
- 流式回复、思考/内心 OS 展示、消息重发与失败恢复。
- 发送超时与断线恢复：无数据超时后自动结束本轮并保留重试入口。
- 看图（Vision）能力与按用途选择模型。
- 语音输入转写与语音气泡播放。

### 记忆
- 长期记忆注入对话（含向量检索可选模块）。
- 记忆候选审核流程：候选 → 待确认 → 已确认，普通编辑不会自动升级为长期事实。
- 生活事件与角色动态的来源链，自动过滤技术性内容。

### 动态与评论
- 动态发布、图片上传、自拍同图动态、定时自动动态。
- 动态去重（冷却窗口内的相似动态不重复发布）。
- 动态评论的打开、发送、失败重试闭环。
- 角色回应动态/评论的可开关能力。

### 形象与表现
- Live2D 播放（Cubism）与按需加载，并提供静态预览、伪 2D 两级回退。
- 立绘取景（半身/全身）与缩放的按角色配置。
- 角色头像、立绘预设与自定义上传。

### 模型与渠道
- BYOK：每位用户配置自己的模型渠道与密钥，项目不内置公共额度。
- 按用途绑定模型（聊天 / 看图 / 图片生成 / 语音）。
- 模型能力自动识别（如 Vision 支持判定）。
- 用量账本与健康页面基础。

### 部署与运维
- 一键 Docker 部署；自托管说明与部署指南。
- PM2 进程配置；Nginx 反向代理示例。
- Android 应用壳（Capacitor）与内置更新检查。
- 隐私优先的经验：凭据不入库、诊断日志脱敏、私有内容与公开代码分离。

---

## 版本号约定（自本文件建立后生效）

- `MAJOR`：不兼容的改动（如数据结构或配置格式变更）；
- `MINOR`：向后兼容的新功能；
- `PATCH`：向后兼容的修复。

发布时请把 `[Unreleased]` 的内容移入新的版本小节，并注明日期。
