# 参与贡献（Contributing）

感谢你有兴趣参与。请先读 [README.md](./README.md) 与 [DISCLAIMER.md](./DISCLAIMER.md)，
后者是本项目的使用条款，**参与贡献即表示你同意它**。

## 先确认你的方向

本项目定位是**个人陪伴型应用框架**，以下方向的改动**不会被接受**：

- 绕过或削弱模型服务商安全策略的功能（越狱、破限、提示词注入类）；
- 面向未成年人或涉及未成年人的任何内容与功能；
- 用于付费陪伴、割韭菜、引流等商业用途的改造（许可证已禁止商业使用）；
- 伪造数据、假统计、虚假亲密度等"看起来像但其实是假的"功能。

## 开发流程

1. **Fork 本仓库**，从 `main` 切出一个分支：`fix/xxx`、`feat/xxx`、`docs/xxx`。
2. **在本地完成开发、构建与测试**，不要在服务器上编译（详见下方"硬性约定"）。
3. 提交前请确保：
   ```bash
   # 后端测试
   cd server && node --test *.test.js
   # 前端测试与构建
   cd frontend-react && npm run build
   ```
   测试未通过或构建失败的改动不会被合并。
4. 发起 Pull Request，说明：**改了什么、为什么改、怎么验证的**。

## 硬性约定（请勿违反）

- **绝不提交任何真实凭据**：`.env`、密码、令牌、私钥、服务器地址、账号。
  仓库内只保留不含真实值的 `server/.env.example`。
- **绝不提交任何一人（或你自己）的私人内容**：聊天原文、私人人设、
  个人经历与统计数字。判断标准见仓库根目录的约定与 `docs` 内说明。
- **绝不提交** `user_assets/`、`_manual_backups/`、`logs/`、
  任何 `*-private.json` 或本地生成的测试产物。
- 提交信息请写**中性的技术描述**（做了什么改动），
  不要写"为什么会有这条规则"的个人故事。

## Pull Request 的期望

- 改动尽量小而聚焦，一个 PR 只做一件事；
- 有行为变化的改动请**附带测试**；
- 涉及用户可见行为的 Bug 修复，请说明复现步骤；
- 不要顺手改无关文件的格式（会污染 diff）。

## 关于"成熟度"的约定

本项目重视**看起来与用起来都是成品**：功能要真、状态要完整、错误要有回退。
如果一个功能只有"能跑"但没有失败状态、空状态或降级路径，请把它补齐再提交。

## 行为准则

- 对事不对人；不嘲讽、不人身攻击、不歧视。
- 不为割韭菜、诈骗、骚扰等用途提供协助。
- 尊重其他使用者与角色的隐私与边界。

## 许可证

贡献的代码将按本项目的 [LICENSE](./LICENSE)（CC-BY-NC-4.0）授权，
即：允许非商业使用，**禁止商业使用**。
