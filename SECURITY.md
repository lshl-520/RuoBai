# 安全政策（Security Policy）

## 报告安全问题

如果你在本项目中发现安全问题（例如：凭据泄露、越权访问、可被利用的注入或
文件上传漏洞、隐私数据未隔离等），请不要公开提交 Issue，改用以下方式私下告知：

- 通过本项目作者 GitHub 主页上的联系方式（见 `README.md` 的「赞助鸣谢」上方仓库主页）；
- 或在 GitHub 仓库页面使用 **Security → Report a vulnerability**（如已开启）。

**请勿在公开 Issue / PR 中附带任何真实凭据、服务器地址或用户数据。**

## 本项目的安全边界（请勿越界）

- 本项目是**自托管**应用，**作者不运营任何公共实例**，也不接触任何使用者的数据。
- 使用者对自己的部署环境的安全负全部责任，包括：服务器加固、证书、
  数据库权限、备份与访问控制。
- 本项目**不含**任何绕过模型服务商安全策略的手段，详见 [DISCLAIMER.md](./DISCLAIMER.md)。

## 项目已采取的设计约定

- **凭据不入库**：`.env`、`高风险/`、`user_assets/` 等均在 `.gitignore` 中；
  仓库内只保留不含真实值的 `server/.env.example`。
- **用户数据隔离**：不同用户与角色之间的聊天、记忆、动态、素材相互隔离。
- **隐私优先的日志**：诊断日志只保留脱敏错误摘要，
  **不记录聊天正文、私人人设、记忆内容、图片、密码、令牌或密钥**。
- **私有内容与公开代码分离**：部署方自己的关系设置、称呼等放在私有配置层
  （见 `docs/部署指南.md` 的「私有关系配置」一节），不进入公开仓库。
- **文件上传校验**：上传的素材会校验路径穿越、非法类型与哈希。

## 提交代码前请自查（贡献者）

1. **不要**提交任何真实凭据、服务器地址、账号、令牌或私钥；
2. **不要**提交 `.env`、`user_assets/`、任何 `*-private.json`；
3. **不要**在提交信息里写入使用者原话、精确统计数字或可识别个人的细节；
4. 若不确定某内容是否适合公开，**默认不提交**，先询问维护者。

## 支持范围

- 只维护**最新版本**。旧版本的问题请先升级后复现。
- 本项目为个人开源项目，安全修复的响应时间不作承诺。
